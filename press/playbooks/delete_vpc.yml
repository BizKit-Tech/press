---
- hosts: localhost
  gather_facts: no
  vars:
    vpc_id: '{{ vpc_id }}'
    route_table_id: '{{ route_table_id }}'
    subnet_cidr: '{{ subnet_cidr }}'
    subnet_2_cidr: '{{ subnet_2_cidr }}'
  roles:
    - role: delete_vpc
